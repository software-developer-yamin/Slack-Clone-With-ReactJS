import styled from "styled-components";

function Chat() {
  return (
    <ChatContainer>
      <h1>Chat</h1>
    </ChatContainer>
  );
}

export default Chat;

const ChatContainer = styled.section``;
